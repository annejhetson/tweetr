<%= form_for [@user, @new_avatar], :html => { :multipart => true }, remote: true do |f| %>
    <%= f.file_field :avatar %>
    <%= f.hidden_field :user_id, value: current_user.id %>
    <%= f.submit %>
<% end %>
